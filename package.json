{
  "name": "code-dep-analyzer",
  "private": true,
  "scripts": {
    "dev:api": "pnpm --filter @dep-analyzer/api dev",
    "dev:web": "pnpm --filter @dep-analyzer/web dev",
    "build": "pnpm --filter @dep-analyzer/core build && pnpm --filter @dep-analyzer/api build && pnpm --filter @dep-analyzer/web build",
    "build:prod": "pnpm --filter @dep-analyzer/core build && pnpm --filter @dep-analyzer/api build && VITE_BASE_URL=/cda/ pnpm --filter @dep-analyzer/web build",
    "test": "pnpm --filter @dep-analyzer/core test",
    "test:coverage": "pnpm --filter @dep-analyzer/core test:coverage"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
